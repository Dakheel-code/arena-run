# تعليمات إعادة تشغيل Discord Bot

## ✅ تم التعديل:
تم تحديث Discord Bot ليعرض **اسم العضو في السيرفر** (Server Nickname) بدلاً من Discord ID في جميع الإشعارات.

## 🔄 لتطبيق التعديلات:

### الطريقة 1: إعادة تشغيل البوت يدوياً

إذا كان البوت يعمل على جهازك المحلي:

```bash
# أوقف البوت الحالي (Ctrl+C)
# ثم شغله مرة أخرى:
cd "C:\Users\PCD\CascadeProjects\Arena Run\discord-bot"
node index.js
```

### الطريقة 2: إعادة تشغيل البوت على السيرفر

إذا كان البوت يعمل على سيرفر (VPS/Cloud):

```bash
# SSH إلى السيرفر
ssh your-server

# اذهب إلى مجلد البوت
cd /path/to/discord-bot

# أوقف البوت
pm2 stop arena-bot
# أو
killall node

# شغل البوت مرة أخرى
pm2 start index.js --name arena-bot
# أو
node index.js
```

## 📋 ما تم تعديله:

1. ✅ البوت الآن يجلب `discord_global_name` من جدول `members`
2. ✅ يستخدم اسم العرض في السيرفر (Server Nickname) بدلاً من Discord ID
3. ✅ يعرض الاسم الصحيح في جميع إشعارات الفيديوهات

## 🎯 النتيجة:

بعد إعادة تشغيل البوت، ستظهر الإشعارات بهذا الشكل:
- **قبل**: `<@942383653269438323>`
- **بعد**: `RAGE` أو `Dakheel ᴿᴳᴿ`

## ⚠️ ملاحظة مهمة:

الإشعارات القديمة التي تم إرسالها بالفعل لن تتغير. التعديلات ستطبق فقط على الإشعارات الجديدة بعد إعادة تشغيل البوت.
