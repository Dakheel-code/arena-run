# 🗄️ تطبيق مخطط قاعدة البيانات (Database Schema)

## 🚨 المشكلة الحالية
```
❌ فشل الاتصال: permission denied for schema public
```
هذا يعني أن الجداول والسياسات لم يتم إنشاؤها بعد.

## 📋 خطوات الحل

### 1. افتح لوحة تحكم Supabase
1. اذهب إلى: https://supabase.com/dashboard
2. سجل الدخول بحسابك
3. اختر مشروع: **dlytcwpwtcfwacktpeur**

### 2. افتح محرر SQL
1. من القائمة الجانبية، اضغط على **"SQL Editor"**
2. اضغط على **"+ New query"** لإنشاء استعلام جديد

### 3. انسخ والصق المخطط
1. افتح ملف: `schema-to-copy.sql`
2. انسخ كل المحتوى (106 سطر)
3. الصقه في محرر SQL

### 4. نفذ المخطط
1. اضغط على زر **"Run"** ▶️
2. انتظر حتى تظهر رسالة **"Success"**

### 5. تحقق من النتائج
بعد التنفيذ، يجب أن ترى:
- ✅ 6 جداول تم إنشاؤها
- ✅ سياسات RLS تم تطبيقها
- ✅ فهارس تم إنشاؤها

## 📊 الجداول التي سيتم إنشاؤها:
- `members` - أعضاء النظام
- `admins` - المشرفين
- `videos` - الفيديوهات
- `view_sessions` - جلسات المشاهدة
- `alerts` - التنبيهات
- `settings` - إعدادات النظام

## 🔍 التحقق بعد التطبيق
بعد تطبيق المخطط، قم بتشغيل:
```bash
node database-reset-scripts.cjs
```

يجب أن ترى:
```
✅ الاتصال ناجح
✅ members: exists
✅ admins: exists
✅ videos: exists
✅ view_sessions: exists
✅ alerts: exists
✅ settings: exists
🎉 كل شيء يعمل بشكل صحيح!
```

## ⚠️ ملاحظات هامة
- **لا تقم** بتعديل المخطط يدوياً
- **احتفظ** بنسخة احتياطية قبل التنفيذ
- **تأكد** من استخدام service_role key للتنفيذ

## 🆘 في حالة وجود مشاكل
إذا ظهرت أخطاء:
1. تحقق من صلاحيات المشروع
2. تأكد من نسخ المخطط بالكامل
3. حاول تنفيذ كل جدول على حدة

---
**بعد تطبيق المخطط، ستكون قاعدة البيانات جاهزة للاستخدام!**
